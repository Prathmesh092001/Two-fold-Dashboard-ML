# My Dashboard Project

## Setup Instructions

### Backend
1. Install dependencies:
   ```bash
   cd backend
   pip install -r requirements.txt


### Initialize the database and load data:
python db_init.py

### Run the Flask application:
python app.py


## Frontend
cd frontend
npm install

### Run the React Application
npm start

### API Endpoints

GET /api/cells: List all cell IDs.

GET /api/cells/<id>: Get data for a specific cell ID.


## Testing
cd backend
python -m unittest discover



#### Step 7: Create a GIF

Record a small GIF showing the dashboard in action using a tool like LICEcap or ShareX. Save the GIF in the project root directory.

#### Step 8: GitHub Repository

1. Initialize a Git repository and commit all changes.
2. Push the repository to GitHub.
3. Submit the GitHub repository link using the provided submission link.

### Final Notes

- Ensure the backend is running on port 8080 and the frontend makes API calls to the correct endpoint.
- Test the entire flow end-to-end to ensure everything works as expected.
- If you have any questions, feel free to reach out via the provided email.

This completes the step-by-step implementation of the assignment. Let me know if you need further details or specific code snippets!
